From: DevBox UML arm64 Port
Subject: [PATCH 8/13] um/os-Linux/start_up.c: arm64 compat for check_ptrace + non-fatal sysemu

arm64 cannot use PTRACE_PEEKUSER/POKEUSER. Adds arm64_check_ptrace() using
PTRACE_GETREGSET + PTRACE_SET_SYSCALL. Makes check_sysemu() non-fatal.
Guards ldt.h with CONFIG_X86. Calls arch_prctl_defaults() to disable PAC keys.

--- a/arch/um/os-Linux/start_up.c
+++ b/arch/um/os-Linux/start_up.c
@@ -17,7 +17,9 @@
 #include <sys/wait.h>
 #include <sys/time.h>
 #include <sys/resource.h>
+#ifdef CONFIG_X86
 #include <asm/ldt.h>
+#endif
 #include <asm/unistd.h>
 #include <init.h>
 #include <os.h>
@@ -27,6 +29,15 @@
 #include <registers.h>
 #include <skas.h>
 #include "internal.h"
+#ifdef __aarch64__
+#include <sys/uio.h>
+#include <elf.h>
+#ifndef PTRACE_SET_SYSCALL
+#define PTRACE_SET_SYSCALL 23
+#endif
+#endif
+
+int sysemu_supported = 0;
 
 static void ptrace_child(void)
 {
@@ -184,6 +195,62 @@
 	fatal("missing\n");
 }
 
+
+#ifdef __aarch64__
+static void __init arm64_check_ptrace(void)
+{
+	int pid, n, status;
+	unsigned long long regs[34];
+	struct iovec iov;
+	unsigned long nr;
+	bool found = false;
+
+	os_info("Checking ptrace syscall modification (arm64 PTRACE_SET_SYSCALL)...");
+	pid = start_ptraced_child();
+
+	if (ptrace(PTRACE_SETOPTIONS, pid, 0,
+		   (void *) PTRACE_O_TRACESYSGOOD) < 0)
+		fatal_perror("arm64_check_ptrace: PTRACE_SETOPTIONS failed");
+
+	while (1) {
+		if (ptrace(PTRACE_SYSCALL, pid, 0, 0) < 0)
+			fatal_perror("arm64_check_ptrace: PTRACE_SYSCALL failed");
+
+		CATCH_EINTR(n = waitpid(pid, &status, WUNTRACED));
+		if (n < 0)
+			fatal_perror("arm64_check_ptrace: waitpid failed");
+
+		if (!WIFSTOPPED(status)) {
+			if (found && WIFEXITED(status) && WEXITSTATUS(status) == 0)
+				break;
+			fatal("arm64_check_ptrace: unexpected child exit, status=0x%x\n",
+			      status);
+		}
+
+		if (WSTOPSIG(status) != (SIGTRAP | 0x80))
+			continue;
+
+		if (found)
+			continue;
+
+		iov.iov_base = regs;
+		iov.iov_len  = sizeof(regs);
+		if (ptrace(PTRACE_GETREGSET, pid, (void *)(long)NT_PRSTATUS, &iov) < 0)
+			fatal_perror("arm64_check_ptrace: PTRACE_GETREGSET failed");
+
+		nr = regs[8];
+		if (nr == __NR_getpid) {
+			if (ptrace(PTRACE_SET_SYSCALL, pid, 0,
+				   (void *)(unsigned long)__NR_getppid) < 0)
+				fatal_perror("arm64_check_ptrace: PTRACE_SET_SYSCALL failed");
+			found = true;
+		}
+	}
+
+	os_info("OK\n");
+}
+#endif /* __aarch64__ */
+
 static void __init check_ptrace(void)
 {
 	int pid, syscall, n, status;
@@ -283,6 +350,10 @@
 {
 	int pid;
 
+#ifdef __aarch64__
+	arch_prctl_defaults();
+#endif
+
 	/* Print out the core dump limits early */
 	check_coredump_limit();
 
